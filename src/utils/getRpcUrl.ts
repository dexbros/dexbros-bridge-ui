// // utils/getRpcUrl.ts
// export const getRpcUrl = (chainId: number): string => {
//   switch (chainId) {
//     case 11155111:
//       return `${import.meta.env.VITE_API_RPC_L1}/l1`; // Sepolia  L1
//     case 220070:
//       return `${import.meta.env.VITE_API_RPC_L2}/l2`; // Dexbros L2
//     default:
//       throw new Error(`Unsupported chainId: ${chainId}`);
//   }
// };
